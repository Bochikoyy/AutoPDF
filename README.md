# AutoPDF
AutoPDF is a Python-based Windows background utility that automatically detects newly downloaded or saved supported files and converts them into PDF. It monitors selected folders, processes files locally, preserves the originals, and works quietly in the background with no manual uploads or conversion steps required.
